version: '3.8'

services:
  web:
    image: ghcr.io/beta-dev64/betabevsurvey:latest
    container_name: flask_backend
    restart: always
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production